# Environment Configuration
NODE_ENV="development" # Options: 'development', 'production'
PORT="8080"            # The port your server will listen on
HOST="localhost"       # Hostname for the server

# CORS Settings
CORS_ORIGIN="http://localhost:8080" # Allowed CORS origin, adjust as necessary

# Rate Limiting
COMMON_RATE_LIMIT_WINDOW_MS="1000"  # Window size for rate limiting (ms)
COMMON_RATE_LIMIT_MAX_REQUESTS="20" # Max number of requests per window per IP

# Database Configuration
# For local development (Docker Compose):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/express_db"
# For AWS RDS (production), use your RDS endpoint:
# DATABASE_URL="postgresql://username:password@your-rds-endpoint.region.rds.amazonaws.com:5432/dbname"

# AWS Cognito Configuration (required for authentication)
AWS_REGION="us-east-1"
AWS_COGNITO_USER_POOL_ID="us-east-1_xxxxxxxxx"  # Replace with your Cognito User Pool ID
AWS_COGNITO_CLIENT_ID="xxxxxxxxxxxxxxxxxxxxxxxxxx"  # Replace with your Cognito Client ID

# AWS SES Configuration (optional, for email invitations)
# AWS_SES_FROM_EMAIL="noreply@yourdomain.com"  # Replace with your verified SES email
