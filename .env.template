# Environment Configuration
NODE_ENV="development" # Options: 'development', 'production', 'test'
PORT="8080"            # The port your server will listen on
HOST="localhost"       # Hostname for the server

# CORS Settings
CORS_ORIGIN="http://localhost:8080" # Allowed CORS origin, adjust as necessary

# Rate Limiting
COMMON_RATE_LIMIT_WINDOW_MS="1000"  # Window size for rate limiting (ms)
COMMON_RATE_LIMIT_MAX_REQUESTS="20" # Max number of requests per window per IP

# ============================================
# Database Configuration
# ============================================
# For local development with Docker Compose (PostgreSQL):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/express_db"

# For production with AWS RDS:
# DATABASE_URL="postgresql://username:password@your-rds-endpoint.region.rds.amazonaws.com:5432/dbname"

# ============================================
# Authentication Configuration
# ============================================
# Set to "true" to bypass authentication in local development (recommended for local dev)
# Set to "false" to enable real AWS Cognito authentication (for testing auth or production)
DISABLE_AUTH="true"

# ============================================
# AWS Configuration
# ============================================
AWS_REGION="us-east-1"
AWS_ACCESS_KEY_ID="test"        # Use 'test' for LocalStack, replace for production AWS
AWS_SECRET_ACCESS_KEY="test"    # Use 'test' for LocalStack, replace for production AWS

# ============================================
# AWS S3 Configuration
# ============================================
AWS_S3_BUCKET_NAME="pet-photos"

# For local development with LocalStack:
AWS_S3_ENDPOINT="http://localhost:4566"  # Remove or comment out for production AWS S3

# For production AWS S3:
# AWS_S3_ENDPOINT=""  # Leave empty or remove this line for real AWS S3

# ============================================
# AWS Cognito Configuration (Authentication)
# ============================================
# Only required when DISABLE_AUTH="false"

# For testing with real AWS Cognito (set DISABLE_AUTH="false"):
# AWS_COGNITO_USER_POOL_ID="us-east-1_YourPoolID"
# AWS_COGNITO_CLIENT_ID="YourClientID"

# For production with AWS Cognito:
# DISABLE_AUTH="false"
# AWS_COGNITO_USER_POOL_ID="us-east-1_RealPoolID"
# AWS_COGNITO_CLIENT_ID="RealClientID"

# ============================================
# AWS SES Configuration (Email Service - Optional)
# ============================================
# For sending email invitations and notifications
# AWS_SES_FROM_EMAIL="noreply@yourdomain.com"  # Must be verified in AWS SES
